# Employee Attrition Frontend

This Next.js frontend uses React and Tailwind CSS to interact with the backend prediction API.

## Setup

```bash
cd frontend
npm install
npm run dev
```

## Development

- Backend endpoint: `http://localhost:8000/predict`
- Frontend proxy: `/api/predict`

## Notes

Make sure the backend is running before using the prediction form.
